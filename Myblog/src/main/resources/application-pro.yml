#生产环境
spring:
    datasource:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://localhost:3306/myblog?characterEncoding=UTF-8&serverTimezone=GMT%2B8&useSSL=false
      username: root
      password: 123456
    jpa:
      hibernate:
        ddl-auto: none
      show-sql: true
logging:
  level:
    root: warn
    com.lc: info
  file: log/blog-pro.log

server:
  port: 8081
